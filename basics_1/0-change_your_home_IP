#!/usr/bin/env bash
#Ejecutar en bash

cp /etc/hosts /etc/hosts.bak

grep -v "127.0.0.2 localhost" /etc/hosts | grep -v "8.8.8.8 facebook.com" > /tmp/hosts_temp

echo "127.0.0.2 localhost" >> /tmp/hosts_temp
echo "8.8.8.8 facebook.com" >> /tmp/hosts_temp

mv /tmp/hosts_temp /etc/hosts
